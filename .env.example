# Application Configuration
PORT=8080
GIN_MODE=debug

# Environment
APP_ENV=development
APP_NAME=shorten-url

# Database (example configuration)
DB_DRIVER=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=shorten_url_db
DB_SSL_MODE=disable

# Redis (example configuration)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# JWT (example configuration)
JWT_SECRET=your-super-secret-jwt-key
JWT_EXPIRES_IN=24h

# External APIs (example)
API_BASE_URL=http://localhost:8080